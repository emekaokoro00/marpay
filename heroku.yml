# Reference the Dockerfiles to build into Docker image
build:
  docker:
    server: marpay/Dockerfile
    web: client/Dockerfile
run:
  server: daphne --bind 0.0.0.0 --port 8000 marpay.asgi:application
  web: ng serve --host 0.0.0.0